{
  "name": "ai-codegen",
  "version": "1.0.0",
  "description": "ai code generation through openai chat",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "env": "^0.0.2",
    "prettier": "^2.8.1"
  }
}
